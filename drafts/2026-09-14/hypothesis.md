This draft tests whether a `truth` hook on a `pipeline` topic — specifically, sources sought notices as the overlooked pre-solicitation signal — outperforms the recent `question` and `story` hooks on `certifications` and `gsa_basics`. The insight is actionable and non-obvious: most small businesses search for open solicitations and arrive too late, while the real window (sources sought phase) passes unnoticed. The prose format strips away all structure to let the single truth land hard. Expected lift: **reactions** from readers who recognize the "halfway run race" feeling, and **comments** from those who want to share their pre-solicitation process or who are hearing about sources sought for the first time. The `single-03-quote` template isolates the closing line as a pull-quote, reinforcing the central claim visually and making the card shareable standalone.
