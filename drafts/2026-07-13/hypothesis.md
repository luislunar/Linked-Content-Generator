This draft tests a **question hook** on a **behind_scenes** post type covering the `sam` topic. The question opener ("When did you last actually audit your SAM.gov profile — not just hit renew?") was required today because the last three posts all opened with statements — forcing a hook rotation — and `question` hadn't been used in at least five days. The format is casual prose with a real process reveal (the 47-point checklist, specific tool: GSA eOffer/eMod, specific field: NAICS code drift), which is the defining feature of `behind_scenes` and typically earns stronger trust signals than abstract advice.

The hypothesis: grounding the post in a named internal tool (eOffer/eMod) and a specific failure mode (NAICS code not updated after adding a service line) will generate **comments** from readers who recognize the same gap in their own registration — either asking "how do I check this?" or sharing a similar experience. The fill-in-the-blank CTA lowers the friction to comment (people complete sentences instinctively). Expected movement: **comments +15–25%** vs. the prior behind_scenes baseline. Reactions should hold steady; this isn't a high-emotion post, it's a useful-and-specific one.
