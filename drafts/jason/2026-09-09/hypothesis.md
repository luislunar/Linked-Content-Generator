This draft tests whether a single concrete federal spending figure — opened bluntly with no preamble — outperforms opinion-based posts on reactions and saves. The $21.9B DoD stat is designed to stop the scroll: it's larger than most readers expect, sourced, and immediately connected to an objection Jason hears in real calls ("we're too small"). Expected score movement: reactions (+) because the number is shareable and surprising; comments (+) because the closing question — "what's the one thing actually standing between you and your first federal award?" — invites real self-diagnosis rather than a generic opinion. The `single-04-stat` template keeps the visual impact on the number itself, which should reinforce the hook rather than dilute it.
