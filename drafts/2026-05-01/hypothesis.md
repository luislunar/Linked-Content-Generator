# Hypothesis — 2026-05-01

Testing an `alert` hook on the `sam` topic in `checklist` format. Last 5 daily runs leaned on `gsa_basics` and `compliance` with `number`/`question`/`story` hooks, so SAM has gone untouched for the visible rotation window — the audience for federal-contracting small businesses is most active at the SAM step and most likely to engage with a "you might be losing contracts and not know it" framing.

The post stays inside the GSA Focus ICP (small businesses already registered, frustrated by invisibility) and avoids the deprecated "comment X to receive" CTA. The community question asks readers to self-audit — that should pull comments from people sharing what they actually found in their own profile, which is the format that historically produces conversation rather than just reactions.

Expected to move: **comments** (specific, share-experience prompt) and **reactions** (alert hook tends to over-index on agreement reacts in B2B GovCon). Lower expectation on raw impressions since SAM is a narrower interest topic than `gsa_basics`.
